apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dispatch-attachments
  namespace: dispatch
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-csi
  resources:
    requests:
      storage: 5Gi
